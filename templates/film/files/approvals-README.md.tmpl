# Approvals

The files in this folder are written by the user (or by a tool under their control), never by the agent.
`project-scope.json` describes the approved scope referenced by `project.json > autonomy.approval_record`.
