# Licenses and provenance

One file per external asset or provider: exact source, version, code license, data license,
rights on the outputs, network/account/cost requirements. A dependency without a record stays `unverified`.

The assets generated by `scene.build` (primitive characters, lantern) are original and free of rights.
