# {{project_name}} - instructions for the agent

Blender production project managed by the **fluidblend** kit (skill `fluidblend`).

- Read `project.json`, `config/permissions.json` and `state/state.json` before any change.
- Go through the `fluidblend` CLI (or the discovered MCP tools): never an improvised `bpy` script on the sources.
- Work on versions (`shots/<shot>/work/vNNN/`); `approved/`, `audio/source/` and the asset sources are protected.
- A result is finished only once the files exist, reopen and carry evidence (`reviews/`, `renders/`, `exports/`, journal).
- On a revision conflict, an exceeded budget or a missing permission: stop and explain, do not work around it.{{unreal_line}}

The skill describes the working modes: `.claude/skills/fluidblend/SKILL.md` or `.agents/skills/fluidblend/SKILL.md`.
